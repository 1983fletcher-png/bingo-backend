# Copy this file to .env or use the visible "env" file. Paste your GitHub token.
# .env and env are gitignored — never commit them.
#
# GITHUB_TOKEN=ghp_your_token_here
#
# Optional: so the agent can sync frontend to the live app without you running the script:
# MUSIC_BINGO_APP_PATH=/full/path/to/music-bingo-app
#
# Music data layer (key, tempo, year, playCount, tags, etc.) — optional.
# MusicBrainz runs with no key. Add Spotify and/or Last.fm for more metadata.
# Spotify: https://developer.spotify.com/dashboard (new apps may be on hold)
# Last.fm (optional; on hold if key creation fails — layer works without it)
# SPOTIFY_CLIENT_ID=your_client_id
# SPOTIFY_CLIENT_SECRET=your_client_secret
# LASTFM_API_KEY=your_lastfm_api_key
#
# Optional: when a venue site returns 403/503 (e.g. Cloudflare), we retry via ScraperAPI so scraping still works.
# Get a key at https://www.scraperapi.com/ (free tier available).
# SCRAPER_API_KEY=your_scraperapi_key
#
# Optional: so ensure-netlify-builds-from-backend.sh can set Netlify build settings (repo + base=frontend) via API.
# NETLIFY_AUTH_TOKEN=your_netlify_personal_access_token
# NETLIFY_SITE_ID=your_site_api_id
#
# Cloudflare R2 (S3-compatible) — for storing uploaded images and assets.
# Get these from Cloudflare Dashboard → R2 → your bucket → Manage R2 API Tokens.
# Use "Jurisdiction specific endpoints" URL if you enabled that (paste as R2_ENDPOINT).
# R2_ACCOUNT_ID=your_cloudflare_account_id
# R2_ACCESS_KEY_ID=your_r2_access_key_id
# R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
# R2_BUCKET_NAME=your_bucket_name
# R2_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# R2_PUBLIC_BASE_URL=https://your-bucket-id.r2.dev
