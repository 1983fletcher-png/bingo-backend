/**
 * Placeholder for the four hospitality builders (Menu, Event, Live Music, Welcome).
 * Full implementations exist in music-bingo-app; port when ready.
 */
import CreatePlaceholder from './CreatePlaceholder';

export default function CreateBuilderPlaceholder() {
  return (
    <CreatePlaceholder
      title="Page builder"
      backTo="/create/hospitality"
      backLabel="← Hospitality"
    />
  );
}
